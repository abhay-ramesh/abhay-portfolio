version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '6359:3000'
    volumes:
      - app-data:/app/node_modules

volumes:
  app-data:

networks:
  default:
    name: web
    external: true
